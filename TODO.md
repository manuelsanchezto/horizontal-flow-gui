2.1. Create the basic tests to allow unit testing for new operations
3. Create the operations for manipulating the variables
    - Modify the value of a variable from the same step (change the initial value)  [X]
    - Modify the value of a variable on another step (change value) [X]
    - Delete completely a variable [X]
    - Delete a variable from a step onwards [ ]
    - Delete a value from the variable [ ]
    - Delete a step [ ]
    - Hide a variable [ ]
    - Re-show a variable [ ]
3.1. Refactor and reduce magic strings
3.2. Introduce the above functionality into the HTML
4. Create a default flow
5. Add a clear flow button
6. Manage storing flows (Import export as json)
7. Add the logic for the evaluation inside the flows
pre-8. Try and set some Flex with tables
8. Add UI manipulations
    - Ordering of steps
    - Ordering of variables
    - IFrames to manage the edition
    - Show/Hide controls
9. Consider a UI-flags option (use horizontal/vertical)


DONE:
1. Try to move the Variables to the Top context level.
2. Create the interaction between steps and variables on the steps level.
